export type Data = {
    storeId: string,
    dataName: string,
    img: string,
    title: string,
    subtitle: string,
    startItem: number,
    perItem: number,
    total: number,
    NumOfChoice: number
}